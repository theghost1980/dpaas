import { Router, Request, Response } from 'express';

const router = Router();

// GET /status/:account - Verifica el estado de un servicio o suscripción
// router.get('/status/:account', (req: Request, res: Response) => {
//   // Lógica para verificar el estado
// });

export default router;