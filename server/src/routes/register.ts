import { Router, Request, Response } from 'express';

const router = Router();

// POST /register - Endpoint para registrar un servicio
// router.post('/register', (req: Request, res: Response) => {
//   // Lógica para registrar el servicio
// });

export default router;